apiVersion: v1
kind: Secret
metadata:
  name: docker-credentials
  namespace: tekton-pipelines
  annotations:
    tekton.dev/docker-0: https://docker.io
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "https://docker.io": {
          "username": "markusjacksond",
          "password": "docker3D3D3",
          "auth": "$(echo -n 'markusjackson:docker3D3D3' | base64)"
        }
      }
    }